# My Learning Journey

## Main Goal 
Become a Junior backend Developer.

## Start Date 
Aug 4, 2026



---
# Stage 00 - foundations & Tools
## Stage Objectives :

- Understand how the internet works, including:
  - Client
  - Server
  - Request
  - Response

- Understand:
  - Domain
  - Hosting
  - DNS

- Understand the  task & difference between:
  - Frontend
  - Backend
  - Full-Stack

- Install and set up VS Code and useful extensions.

- Learn the basic terminal commands:
  - `cd`
  - `ls`
  - `mkdir`
  - `touch`

- Learn the basic Git commands:
  - `git init`
  - `git add`
  - `git commit`
  - `git push`
  - `git clone`

- Create a GitHub account.

- Create the `my-learning-journey` repository.

- Write and update this README file.

---
---

# Learning Log
### August 4, 2026 - 

- Started my learning journey.
- Learn the difference between Git, GitHub & cmder  .
- Created my GitHub account.
- Created the `my-learning-journey` repository.
- Installed Cmder.
- Learned how to clone a repository using Git through Cmder.
- Cloned the repository from GitHub to my laptop.
- Encountered an SSL certificate issue while cloning the repository and learned how to resolve it by changing Git's SSL backend from Schannel to OpenSSL.
- Started documenting my learning journey in this README file.
 
## August 5, 2026


- Configured Git by setting `user.name` and `user.email`.
- Learned how Git configuration works using `git config` and the `--global` option.
- Initialized a local Git repository using `git init`.
- Practiced essential terminal commands (`pwd`, `ls`, `ls -a`, and `explorer .`).
- Generated an SSH key pair, added the public key to GitHub, and verified the SSH connection.
- Connected a local repository to GitHub using `git remote add origin`.
- Understood the difference between HTTPS and SSH authentication.
- Explored the commit history using `git log`.
- Learned how branches work using `git branch`, `git branch -a`, and `git checkout`.
- Understood the difference between `git push`, `git fetch`, and `git pull`.
- Learned the concepts of Fork, Pull Request, Merge, Reset, and Revert.
- Understood the difference between GUI, CLI, Terminal, Shell, and Bash.
- Learned how commands are structured using Commands, Arguments, and Options.
- Practiced navigation using paths and understood Absolute vs Relative Paths.
- Learned how to use `man` pages for command documentation.

## August 6, 2026

* Started learning Networking fundamentals and how the Internet works.
* Learned the Client-Server model and how devices communicate through Requests and Responses.
* Understood the role of Domains, DNS, and Hosting in accessing websites.
* Learned the difference between Internet and Web, and the basic structure of a URL.
* Understood the process of loading a website: Domain → DNS → IP Address → Server → Response → Web Page.
*  Learned the difference between Frontend, Backend, and Full-Stack Development.
* Understood the responsibilities and workflow of each development path.
* Learned how Frontend and Backend work together to build web applications.
---

## Current Status

**Current Stage:** Stage 00 — Foundations and Tools  
**Status:** Completed  
**Completed:** August 10, 2026
  ---
  ----

# Stage 01 - HTML

## Stage Objectives :

- HTML document structure and Meta Tags, including their relation to SEO.
- Headings, paragraphs, text, and lists.
- Links, images, and file paths.
- Semantic HTML:
  - `header`
  - `nav`
  - `main`
  - `section`
  - `article`
  - `footer`
- Tables.
- Forms with different input types and labels.
- Validation attributes:
  - `required`
  - `pattern`
  - `min`
  - `max`
- Basic Accessibility and ARIA.

## Projects

- **Complete CV Page**
  - Build a complete CV page using HTML only, without CSS.
  - Build a complete registration form including:
    - Name
    - Email
    - Password
    - Date of Birth
    - Select menu
    - Radio buttons
    - Checkbox
    - File upload
    - Validation

---

# Learning Log

### August 16, 2026

- Started learning HTML and its role in structuring web pages.
- Learned that HTML stands for HyperText Markup Language.
- Learned about HTML tags, elements, and attributes.
- Learned how to create and edit HTML files using VS Code.
- Learned about `index.html` and the `.html` file extension.
- Practiced viewing page source and inspecting HTML elements using browser developer tools.

### August 20, 2026

- Learned the basic structure of an HTML document.
- Learned the purpose of `<!doctype html>` and its relation to HTML5.
- Understood the difference between the `<head>` and `<body>` sections.
- Learned about Meta Tags, including character encoding and page descriptions.
- Learned how HTML comments are written using `<!-- -->`.
- Learned headings from `<h1>` to `<h6>` and paragraphs using `<p>`.
- Learned about global attributes.

### August 21, 2026

- Learned how to create and structure different types of HTML lists.
- Learned ordered lists using `<ol>` and list items using `<li>`.
- Learned the `reversed`, `start`, and `type` attributes for ordered lists.
- Learned how to use the `value` attribute with `<li>`.
- Practiced creating nested lists.
- Learned description lists using `<dl>`, `<dt>`, and `<dd>`.

### August 22, 2026

- Learned the basic structure of HTML tables.
- Learned how to use `<table>`, `<tr>`, `<td>`, and `<th>`.
- Learned how to organize tables using `<thead>`, `<tbody>`, and `<tfoot>`.
- Practiced creating and structuring table content.

### August 23, 2026

- Continued learning how to structure HTML pages.
- Learned about semantic HTML and why meaningful elements are used to describe page structure.
- Learned and practiced using `<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, and `<footer>`.
- Continued practicing HTML by building and organizing page content.

### August 24, 2026

- Learned how to create HTML forms and structure form controls.
- Learned how to associate `<label>` elements with form controls.
- Practiced different input types and form controls.
- Learned validation attributes including `required`, `pattern`, `min`, and `max`.
- Learned the basics of HTML accessibility and ARIA.
- Continued practicing forms and applying validation rules.

### August 25, 2026

- Continued practicing HTML forms and different form controls.
- Worked with text, email, password, date, radio, checkbox, select, and file input types.
- Practiced combining labels, inputs, and validation attributes to create complete forms.
- Reviewed the HTML concepts learned throughout Stage 01.
- Completed Stage 01 — HTML.

---

## Current Status

**Current Stage:** Stage 01 — HTML  
**Status:** Completed
